<TreeView ItemsSource="${1:Source}">
  <TreeView.ItemTemplate>
    <HierarchicalDataTemplate DataType="${2:DataType}" ItemsSource="{Binding Children}">
        <TextBlock Text="{Binding Name}" />
    </HierarchicalDataTemplate>
  </TreeView.ItemTemplate>
</TreeView>

<ScrollViewer VerticalScrollBarVisibility="Auto" CanContentScroll="True">

<Viewbox Stretch="${1:Uniform}">${2}
</Viewbox>

    <Border 
      BorderThickness="$1{1,1,1,1}" 
      BorderBrush="$1{Black}" 
      CornerRadius="$1{3,3,3,3}" 
      Background="$1{White}">

      <ListBox SelectionMode="${1:Single}" ItemsSource="${2}"/>



          <Slider 
            Minimum="0" Maximum="100" 
            SmallChange="1" LargeChange="10"
            TickPlacement="BottomRight"/>

          <ProgressBar
             Minimum="0" Maximum="200" Value="100"/>

<ToolBarTray>
  <ToolBar Band="1">${1}
  </ToolBar>
</ToolBarTray>



    <StatusBar>
      <StatusBarItem DockPanel.Dock="Left">${1
      </StatusBarItem>
      <Separator     DockPanel.Dock="Left"/>
    </StatusBar>

    <SolidColorBrush x:Key="${1:BrushName}" Color="${2:#FFFFFF}"/>

    <LinearGradientBrush x:Key="${1:BrushName}"
      StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="Yellow"    Offset="0.0" />
      <GradientStop Color="Red"       Offset="0.25" />
      <GradientStop Color="Blue"      Offset="0.75" />
      <GradientStop Color="LimeGreen" Offset="1.0" />
    </LinearGradientBrush>

    <RadialGradientBrush x:Key="${1:BrushName}"
      GradientOrigin="0.5,0.5" Center="0.5,0.5" 
      RadiusX="0.5" RadiusY="0.5">
      <GradientStop Color="Yellow" Offset="0" />
      <GradientStop Color="Red" Offset="0.25" />
      <GradientStop Color="Blue" Offset="0.75" />
      <GradientStop Color="LimeGreen" Offset="1" />
    </RadialGradientBrush>

    <ImageBrush ImageSource="${1}"  />

    <Style x:Key="XXXX$1" TargetType="{x:Type ${1:Button}}" BasedOn="{x:Null}">
      <Setter Property="Background" Value="{StaticResource $1Brush}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type $1}">
            <Grid x:Name="Grid">
              <Border x:Name="Border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}"/>
              <ContentPresenter
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Margin="{TemplateBinding Padding}"
                RecognizesAccessKey="True"/>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsPressed" Value="true">
                <Setter Property="Background" Value="{StaticResource PressedBrush}" TargetName="Border"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
